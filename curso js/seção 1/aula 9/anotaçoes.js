/* 

Funçoes dentro do objetivo windowm = metodos

alert = valor undefined
confirm = valor booleand ( true ou false)
prompt = valor definido pelo usuario ( sempre será uma string, para converter necessario usar o parseFloat ou Number) ex: variavel = Number(variavel) ;

os metodos podem ser usados para definir o valor de uma variavel ou constante, ex:

let confirma = confirm('mensagem de texto aqui')  
let caixa = prompt('mensagem aki...')

*/